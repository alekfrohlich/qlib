Document 1: Memory Model

Introduction:
    Qlib's memory model is flat (i.e: no virtual memory and no segmenetation).
    Support for system-specific memory schemes (e.g: memory mapped registers
    and bit-banding regions) comes from the appropriate machine hardware
    mediator.
